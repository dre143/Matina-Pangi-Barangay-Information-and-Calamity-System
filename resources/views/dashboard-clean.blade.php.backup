@extends('layouts.app-clean')

@section('title', 'Dashboard - Barangay Matina Pangi')

@section('content')
<!-- Page Header -->
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h2 class="mb-1"><i class="bi bi-speedometer2 me-2" style="color: var(--primary-green);"></i>Dashboard</h2>
        <p class="text-muted mb-0">Welcome back, {{ auth()->user()->name }}!</p>
    </div>
    <div class="text-muted">
        <i class="bi bi-calendar3"></i> {{ now()->format('F d, Y') }}
    </div>
</div>

<!-- Statistics Cards -->
<div class="row g-4 mb-4">
    <!-- Total Residents -->
    <div class="col-md-3">
        <div class="card stat-card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="stat-icon me-3">
                        <i class="bi bi-people-fill"></i>
                    </div>
                    <div class="flex-grow-1">
                        <h6 class="text-muted mb-1">Total Residents</h6>
                        <h3 class="mb-0">{{ $stats['total_residents'] ?? 0 }}</h3>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Total Households -->
    <div class="col-md-3">
        <div class="card stat-card">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="stat-icon me-3">
                        <i class="bi bi-house-door-fill"></i>
                    </div>
                    <div class="flex-grow-1">
                        <h6 class="text-muted mb-1">Total Households</h6>
                        <h3 class="mb-0">{{ $stats['total_households'] ?? 0 }}</h3>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Senior Citizens -->
    <div class="col-md-3">
        <div class="card stat-card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="text-muted mb-1">Senior Citizens</h6>
                        <h3 class="mb-0">{{ $stats['total_senior_citizens'] ?? 0 }}</h3>
                    </div>
                    <div class="text-warning" style="font-size: 48px;">
                        <i class="bi bi-person-cane"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- PWD -->
    <div class="col-md-3">
        <div class="card stat-card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="text-muted mb-1">PWD</h6>
                        <h3 class="mb-0">{{ $stats['total_pwd'] ?? 0 }}</h3>
                    </div>
                    <div class="text-info" style="font-size: 48px;">
                        <i class="bi bi-universal-access"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Secondary Stats -->
<div class="row g-4 mb-4">
    <!-- Teens -->
    <div class="col-md-3">
        <div class="card stat-card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="text-muted mb-1">Teens (13-19)</h6>
                        <h3 class="mb-0">{{ $stats['total_teens'] ?? 0 }}</h3>
                    </div>
                    <div class="text-info" style="font-size: 48px;">
                        <i class="bi bi-person-standing"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Voters -->
    <div class="col-md-3">
        <div class="card stat-card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="text-muted mb-1">Voters</h6>
                        <h3 class="mb-0">{{ $stats['total_voters'] ?? 0 }}</h3>
                    </div>
                    <div class="text-success" style="font-size: 48px;">
                        <i class="bi bi-check2-circle"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- 4Ps -->
    <div class="col-md-3">
        <div class="card stat-card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="text-muted mb-1">4Ps Beneficiaries</h6>
                        <h3 class="mb-0">{{ $stats['total_4ps'] ?? 0 }}</h3>
                    </div>
                    <div class="text-warning" style="font-size: 48px;">
                        <i class="bi bi-cash-coin"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Avg Household Size -->
    <div class="col-md-3">
        <div class="card stat-card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="text-muted mb-1">Avg Household Size</h6>
                        <h3 class="mb-0">{{ number_format($stats['average_household_size'] ?? 0, 1) }}</h3>
                    </div>
                    <div class="text-primary" style="font-size: 48px;">
                        <i class="bi bi-people"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Recent Activity & Charts -->
<div class="row g-4">
    <!-- Recent Residents -->
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <i class="bi bi-clock-history me-2"></i>Recently Added Residents
            </div>
            <div class="card-body p-0">
                @if(isset($recentResidents) && $recentResidents->count() > 0)
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <tbody>
                                @foreach($recentResidents as $resident)
                                <tr>
                                    <td>
                                        <strong>{{ $resident->full_name }}</strong><br>
                                        <small class="text-muted">{{ $resident->resident_id }}</small>
                                    </td>
                                    <td>
                                        <span class="badge badge-{{ $resident->status }}">
                                            {{ ucfirst($resident->status) }}
                                        </span>
                                    </td>
                                    <td class="text-end">
                                        <a href="{{ route('residents.show', $resident) }}" class="btn btn-sm btn-outline-success">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                    </td>
                                </tr>
                                @endforeach
                            </tbody>
                        </table>
                    </div>
                @else
                    <div class="p-4 text-center text-muted">
                        <i class="bi bi-inbox fs-1 d-block mb-2"></i>
                        No recent residents
                    </div>
                @endif
            </div>
        </div>
    </div>

    <!-- Quick Stats -->
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <i class="bi bi-bar-chart-fill me-2"></i>Population Overview
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <div class="d-flex justify-content-between mb-1">
                        <span>Male</span>
                        <strong>{{ $stats['male_count'] ?? 0 }}</strong>
                    </div>
                    <div class="progress" style="height: 8px;">
                        <div class="progress-bar" style="width: {{ $stats['total_residents'] > 0 ? ($stats['male_count'] / $stats['total_residents'] * 100) : 0 }}%; background-color: var(--primary-green);"></div>
                    </div>
                </div>

                <div class="mb-3">
                    <div class="d-flex justify-content-between mb-1">
                        <span>Female</span>
                        <strong>{{ $stats['female_count'] ?? 0 }}</strong>
                    </div>
                    <div class="progress" style="height: 8px;">
                        <div class="progress-bar" style="width: {{ $stats['total_residents'] > 0 ? ($stats['female_count'] / $stats['total_residents'] * 100) : 0 }}%; background-color: #7AE582;"></div>
                    </div>
                </div>

                <hr>

                <div class="row text-center mt-3">
                    <div class="col-4">
                        <h5 class="mb-0">{{ $ageDistribution['children'] ?? 0 }}</h5>
                        <small class="text-muted">Children</small>
                    </div>
                    <div class="col-4">
                        <h5 class="mb-0">{{ $ageDistribution['adults'] ?? 0 }}</h5>
                        <small class="text-muted">Adults</small>
                    </div>
                    <div class="col-4">
                        <h5 class="mb-0">{{ $ageDistribution['seniors'] ?? 0 }}</h5>
                        <small class="text-muted">Seniors</small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Quick Actions -->
<div class="row g-4 mt-2">
    <div class="col-md-4">
        <a href="{{ route('residents.create') }}" class="card text-decoration-none">
            <div class="card-body text-center py-4">
                <i class="bi bi-person-plus-fill fs-1 mb-2" style="color: var(--primary-green);"></i>
                <h5>Register New Resident</h5>
                <p class="text-muted mb-0">Add a new resident to the system</p>
            </div>
        </a>
    </div>

    <div class="col-md-4">
        <a href="{{ route('households.create') }}" class="card text-decoration-none">
            <div class="card-body text-center py-4">
                <i class="bi bi-house-add-fill fs-1 mb-2" style="color: var(--primary-green);"></i>
                <h5>Register New Household</h5>
                <p class="text-muted mb-0">Add a new household record</p>
            </div>
        </a>
    </div>

    <div class="col-md-4">
        <a href="#" class="card text-decoration-none">
            <div class="card-body text-center py-4">
                <i class="bi bi-file-earmark-bar-graph-fill fs-1 mb-2" style="color: var(--primary-green);"></i>
                <h5>Generate Report</h5>
                <p class="text-muted mb-0">Export census and statistics</p>
            </div>
        </a>
    </div>
</div>
@endsection
